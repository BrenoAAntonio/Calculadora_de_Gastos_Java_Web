/* css/style.css */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #121212; /* Dark background */
    color: #e0e0e0; /* Light text */
    margin: 0;
    padding: 0;
    display: flex;
    min-height: 100vh;
    box-sizing: border-box;
}

.body-content {
    display: flex;
    flex-grow: 1;
}

.container {
    flex-grow: 1;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.glass-container {
    background: rgba(30, 30, 30, 0.8); /* Darker glass */
    border-radius: 16px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(37, 37, 37, 0.5);
    padding: 30px;
    margin-bottom: 20px;
    width: 80%;
    max-width: 900px;
    box-sizing: border-box;
}

h1 {
    color: #bb86fc; /* Purple accent */
    text-align: center;
    margin-bottom: 30px;
}

.dashboard-info {
    display: flex;
    justify-content: space-around;
    margin-bottom: 30px;
    width: 100%;
}

.info-card {
    background: rgba(40, 40, 40, 0.7);
    border-radius: 10px;
    padding: 20px;
    width: 30%;
    text-align: center;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(50, 50, 50, 0.3);
}

.info-card h2 {
    color: #03dac6; /* Teal accent */
    margin-top: 0;
    margin-bottom: 10px;
}

.info-card p, .info-card ul {
    margin: 0;
    padding: 0;
    list-style: none;
}

.info-card ul li {
    color: #e0e0e0;
    margin-bottom: 5px;
}

.recent-expenses, .category-spending {
    margin-top: 30px;
    width: 100%;
}

.recent-expenses h2, .category-spending h2 {
    color: #bb86fc; /* Purple accent */
    text-align: left;
    margin-bottom: 15px;
}

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 10px;
    color: #e0e0e0;
}

th, td {
    padding: 10px;
    border-bottom: 1px solid #333;
    text-align: left;
}

th {
    background-color: #2c2c2c;
    color: #03dac6; /* Teal accent */
}

.sidebar {
    width: 200px;
    background: rgba(30, 30, 30, 0.8);
    border-radius: 16px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(37, 37, 37, 0.5);
    padding: 20px;
    margin: 20px;
    display: flex;
    flex-direction: column;
    align-items: stretch;
}

.sidebar h2 {
    color: #bb86fc; /* Purple accent */
    text-align: center;
    margin-bottom: 15px;
}

.sidebar a {
    padding: 10px 15px;
    margin-bottom: 10px;
    background-color: #03dac6; /* Teal accent */
    color: #121212; /* Dark text on accent */
    text-decoration: none;
    border-radius: 5px;
    text-align: center;
    display: block;
    transition: background-color 0.3s ease;
}

.sidebar a:hover {
    background-color: #00b8d4; /* Lighter teal on hover */
}

.main-content {
    flex-grow: 1;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
}